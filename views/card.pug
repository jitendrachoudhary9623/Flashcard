extends layout.pug

block content
  section#content
    h2=text
    if hint
      p
        i Hint: #{hint}
    a(href=`${id}?side=${url}`) #{change} <!-- literal synatx of js -->
    br
    a(href='/cards') Random Card
    